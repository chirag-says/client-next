import AddPropertyWrapper from './AddPropertyWrapper';

export const metadata = {
    title: 'Add Property',
    description: 'List your property for sale or rent on DealDirect — India\'s no-brokerage real estate platform.',
    robots: { index: false, follow: false },
};

export default function AddPropertyPage() {
    return <AddPropertyWrapper />;
}
